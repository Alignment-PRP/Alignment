
const authReducer = (state = {
                          tables: []
                      }, action) => {
    switch (action.type) {
        case TABLE_PAGE:
        default:
            return state;
    }
};

export default authReducer;